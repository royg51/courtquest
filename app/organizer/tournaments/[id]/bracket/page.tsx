// Organizer bracket management page.
// Generate bracket, view bracket, enter scores.
// Implemented in Step 6 (Bracket) + Step 7 (Scoring).

export default function OrganizerBracketPage({
  params,
}: {
  params: { id: string };
}) {
  return <div>Bracket for {params.id} — not yet implemented</div>;
}
