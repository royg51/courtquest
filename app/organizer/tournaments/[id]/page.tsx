// Organizer tournament overview page.
// Shows registration count, status, quick links to sub-pages.
// Implemented in Step 4 (Tournament creation).

export default function OrganizerTournamentPage({
  params,
}: {
  params: { id: string };
}) {
  return <div>Tournament {params.id} overview — not yet implemented</div>;
}
