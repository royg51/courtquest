// Organizer registration management page.
// Lists all teams with status controls: confirm, waitlist, withdraw.
// Implemented in Step 5 (Registration flow).

export default function RegistrationsPage({
  params,
}: {
  params: { id: string };
}) {
  return <div>Registrations for {params.id} — not yet implemented</div>;
}
