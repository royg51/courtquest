// Tournament list page — public, server component.
// Filterable by status, format, and sport.
// Implemented in Step 3 (Tournament listing).

export default function TournamentsPage() {
  return <div>Tournaments list — not yet implemented</div>;
}
